# Copy to .env (gitignored). Before the vault exists, keep secrets in your personal password manager.

# Permanent URL and its host name. Never change after users are enrolled.
APP_FULL_BASE_URL=https://vault.example.com
VAULT_HOST=vault.example.com

PASSBOLT_IMAGE_TAG=latest-ce
MARIADB_IMAGE_TAG=10.11
TRAEFIK_IMAGE_TAG=v3
PASSBOLT_DB_PASSWORD=

# TLS: Let's Encrypt via Cloudflare DNS-01. Token scoped to the domain's zone: Zone:Read + DNS:Edit.
ACME_EMAIL=
CLOUDFLARE_DNS_API_TOKEN=
# While testing, use staging to avoid rate limits: https://acme-staging-v02.api.letsencrypt.org/directory
ACME_CA_SERVER=https://acme-v02.api.letsencrypt.org/directory

# Loopback address the vault listens on; the hosts file maps vault.<domain> to it.
# If 127.0.0.1:443 is taken, use another loopback address (127.0.0.2) instead of another port:
# the URL stays port-less and portable. macOS needs: sudo ifconfig lo0 alias 127.0.0.2
BIND_ADDR=127.0.0.1
# Last resort only: a non-443 port becomes part of the permanent APP_FULL_BASE_URL (https://vault.<domain>:<port>).
HTTPS_PORT=443

# SMTP (SETUP.md Phase 4). Leave host empty until then.
EMAIL_DEFAULT_FROM=
EMAIL_TRANSPORT_DEFAULT_HOST=
EMAIL_TRANSPORT_DEFAULT_PORT=587
EMAIL_TRANSPORT_DEFAULT_USERNAME=
EMAIL_TRANSPORT_DEFAULT_PASSWORD=
EMAIL_TRANSPORT_DEFAULT_TLS=true
